# Database Configuration
DB_TYPE=postgres
# DB_TYPE=mysql

# PostgreSQL Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=avenir_user
POSTGRES_PASSWORD=avenir_password
POSTGRES_DB=avenir_db

# MySQL Configuration
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=avenir_user
MYSQL_PASSWORD=avenir_password
MYSQL_DB=avenir_db

# Email Configuration
# Pour le développement, utiliser Mailpit (docker-compose)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="AVENIR Bank <noreply@avenir-bank.com>"

# Pour la production, utiliser un vrai serveur SMTP (Gmail, SendGrid, etc.)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Frontend URL
FRONTEND_URL=http://localhost:3000

# JWT Configuration
JWT_ACCESS_SECRET=your-super-secret-access-key-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production

# Server Configuration
PORT=3001
HOST=0.0.0.0
